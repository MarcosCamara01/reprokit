{
  "name": "reprokit",
  "version": "0.1.0",
  "private": true,
  "description": "Issue Repro & Fix Agent — turns vague bug reports into reproducible evidence and safe fix PRs. Built on Vercel Eve.",
  "type": "module",
  "engines": {
    "node": ">=24.0.0"
  },
  "scripts": {
    "dev": "eve dev",
    "build": "eve build",
    "webhook": "tsx src/server.ts",
    "cli": "tsx src/cli.ts",
    "typecheck": "tsc --noEmit",
    "test": "vitest run",
    "test:watch": "vitest"
  },
  "dependencies": {
    "@ai-sdk/google": "^3.0.83",
    "eve": "0.11.3",
    "zod": "^4.4.3"
  },
  "devDependencies": {
    "@types/node": "^22.0.0",
    "microsandbox": "^0.5.7",
    "tsx": "^4.19.0",
    "typescript": "^5.6.0",
    "vite": "^8.0.16",
    "vitest": "^4.1.9"
  }
}
