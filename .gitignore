node_modules/
.env
.env.local
.env.*.local

# Per-issue agent run workspaces (clones, logs, reports). Never commit these.
.runs/

# Local Claude Code overrides (personal full-access mode — do not commit)
.claude/settings.local.json

# SDD development scaffolding — kept local, not published. The agent's own
# bug-fix SDD protocol lives inlined in src/workers/prompts.ts and is unaffected.
.sdd/
.agents/
.claude/commands/
specs/
CLAUDE.md
AGENTS.md

# Build / tooling
dist/
.output/
.eve/
.workflow-data/
.vercel/
*.log
.DS_Store
